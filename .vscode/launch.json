{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "C/C++: clang 生成活动文件",
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}